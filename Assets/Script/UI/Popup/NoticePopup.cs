using UnityEngine;

public class NoticePopup : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("NoticePopup Start");
    }

    // Update is called once per frame
    void Update()
    {

    }
}
